<odoo>
    <!-- explicit list view definition -->

    <!-- <record model="ir.actions.act_window.view" id="action_server">
      <field name="name">vido_crm</field>
      <field name="res_model">model.student</field>
      <field name="view_mode">graph</field>
      <field name="view_id" ref="my_specific_view"/>
    </record> -->


    <!-- actions opening views on models -->
    <record id="student_action" model="ir.actions.act_window">
      <field name="name">vidoClass</field>
      <field name="res_model">student</field>
      <field name="view_mode">tree,form</field>
      <field name="domain">[]</field>
      <field name="context">{}</field>
      <field name="help" type="html">
        <p class="o_view_nocontent_smiling_face">
          create student!
        </p>
      </field>
    </record>
    <!-- server action to the one above -->

    <!-- <record model="ir.actions.server" id="vido_crm.action_server">
      <field name="name">VidoCrm server</field>
      <field name="model_id" ref="model_vido_crm_vido_crm"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": models.student,
        }
        <p class="o_view_nocontent_smiling_face">
            create frist student!?
        </p>
      </field>
    </record> -->

  <menuitem
    id="Class_menu"
    name="Class"
    sequence="0"/>
  
  <menuitem
    id="Student_Class_root"
    name="Student"
    parent="Class_menu"
    action="student_action"
    sequence="10"/>
</odoo>